<div class="container mt-3">
  <div class="row">
    <div class="mx-auto">
    <h2 class="font-weight-bold mb-3"><%= @facility.name %>献立</h2>
    </div>
  </div>

  <%= render 'customer/searches/menu_search' %>
  <%= render 'index', menus: @menus, facility: @facility %>

  <div class="row mt-3 mb-5">
    <div class="mx-auto">
      <%= paginate @menus %><br><br>
      <%= link_to "戻る", :back, class: "btn btn-outline-secondary px-4 mr-5" %>
    </div>
  </div>
</div>